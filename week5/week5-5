# https://www.acmicpc.net/problem/1357

X, Y = input().split()  # 두 숫자 입력(문자열로)

r_X = int(X[::-1])  # 순서 뒤집어서 정수형으로
r_Y = int(Y[::-1])

r_sum = str(r_X + r_Y)[::-1]  # 합을 문자열로 바꿔서 순서 뒤집음

print (int(r_sum)) # 앞에 0 없애기 위해 정수형으로
