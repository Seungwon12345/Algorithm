# https://www.acmicpc.net/problem/10871

N, X = map(int, input('정수 N, X 입력').split())
A = list(map(int, input('정수 N개의 수열 입력').split()))

answer = ""
for i in range(len(A)):
    if A[i] < X:
        answer += str(A[i]) + " "
print(answer)
