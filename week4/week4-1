# 문제 설명
# 당신은 N개의 숫자 카드를 가지고 있습니다.
# 이 카드들 중 다음 조건을 모두 만족하는 카드들을 찾아서 그 카드의 **순서 (몇 번째 카드인지)**와 숫자를 출력하고, 
# 마지막에 조건을 만족하는 카드의 총 개수를 출력하는 프로그램을 작성하세요.

# 카드 조건:
# 카드의 숫자가 10 이상이어야 합니다.
# 카드의 숫자가 3의 배수이면서 홀수여야 합니다.
 

# 입력 방식:
# 첫째 줄에 카드 목록의 길이 N이 주어집니다. (1 이상 100 이하)
# 그 다음 N개의 줄에 걸쳐, 한 줄에 하나씩 카드 숫자가 주어집니다.


N = int(input('받을 카드 개수: '))
count = 0

for i in range(1, N+1):
    card_number = int(input('카드 숫자 입력: '))

    if card_number >= 10 and card_number % 3 == 0 and card_number % 2 == 1:
        print(f'순서: {i}, 숫자: {card_number}')
        count +=1

print(f'총 {count}개 카드 발견')
